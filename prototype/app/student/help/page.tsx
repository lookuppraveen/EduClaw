import { StubPage } from "@/components/layout/StubPage";

export default function Page() {
  return (
    <StubPage
      title="Help / Trust & Safety brief"
      surface="Web"
      role="Student"
      weekTarget={6}
      notes="Plain-language explainer of what EduClaw is and isn't, how Validation works, where to escalate concerns, FERPA stance."
    />
  );
}
