import { StubPage } from "@/components/layout/StubPage";

export default function Page() {
  return (
    <StubPage
      title="Settings"
      surface="Web"
      role="Student"
      weekTarget={6}
      notes="Preferences, accessibility (high-contrast, reduced motion, font size), language, notification cadence."
    />
  );
}
