import { StubPage } from "@/components/layout/StubPage";

export default function Page() {
  return (
    <StubPage title="Faculty settings" surface="Web" role="Faculty" weekTarget={12} notes="Notification preferences, default policy templates, integrations." />
  );
}
