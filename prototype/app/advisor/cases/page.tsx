import { StubPage } from "@/components/layout/StubPage";

export default function Page() {
  return (
    <StubPage title="Student cases" surface="Web" role="Advisor" weekTarget={9} notes="Filterable list of assigned students with risk indicators, last contact, agent activity summary (consent-gated)." />
  );
}
